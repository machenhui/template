@defmixin gradient(FROM_POS,TO_POS, FROM_COLOR, TO_COLOR) {
  background-color: FROM_COLOR; /* fallback color if gradients are not supported */
  background-image:-webkit-gradient(
		linear,
		FROM_POS,
		TO_POS,
		from(FROM_COLOR),
		to(TO_COLOR)
	);
 
}